// This is a manifest file that'll be compiled into application.js, which will include all the files
// listed below.
//
// Any JavaScript/Coffee file within this directory, lib/assets/javascripts, vendor/assets/javascripts,
// or vendor/assets/javascripts of plugins, if any, can be referenced here using a relative path.
//
// It's not advisable to add code directly here, but if you do, it'll appear at the bottom of the
// compiled file.
//
// Read Sprockets README (https://github.com/sstephenson/sprockets#sprockets-directives) for details
// about supported directives.
//
//= require jquery
//= require jquery_ujs
//= require turbolinks
//= require_tree .

//= require bootstrap-sprockets


$(document).ready(function(){
//   $(".movie-description").ellipsis();
	
	// $("#search_user_form").on("submit", function(event){
		// var queryString = $("#searchQ").val();
		// event.preventDefault();
	// 	$.ajax({
	// 		type: "GET",
	// 		url: "/users",
	// 		dataType: "json",
	// 		data: {search: queryString}
	// 	})
	// 	.done(function(response){
	// 		console.log(response.data);
	// 		for(var i in response.data){
	// 			console.log(response.data[i].firstname);
	// 			console.log(response.data[i].id);
	// 			$("#user_search_result").append("<a href='/users/" + response.data[i].id + "'>" + response.data[i].firstname + " " + response.data[i].lastname + "</a></br>");
	// 		}
	// 	})
	// })
});

