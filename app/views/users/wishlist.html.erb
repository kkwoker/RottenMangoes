<h1>
<%=@user.full_name%>
</h1>

<ul class="nav nav-tabs" role="tablist">
  <li><%= link_to "Watched", user_path(@user)%></a></li>
  <li class="active"><%= link_to "Wishlist", wishlist_path(@user)%></a></li>
</ul>

<table>
	<tbody>
		<% @movies.each do |movie| %>
		 <%= render partial: 'movies/movie', locals: {movie: movie} %>
		<% end %>
	</tbody>
</table>